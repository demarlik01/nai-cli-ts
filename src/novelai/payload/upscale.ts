export interface BuildUpscalePayloadInput {
  scale: number;
}

export function buildUpscalePayload(_input: BuildUpscalePayloadInput): never {
  throw new Error("buildUpscalePayload is planned for Phase 2.");
}
